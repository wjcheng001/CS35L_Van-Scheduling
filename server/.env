MONGO_URI="mongodb+srv://celinee:Fev1w0v3rN7Q9KD7@cluster0.rf7cl0z.mongodb.net/?retryWrites=true&w=majority&appName=Cluster"
---

### 3. **Run and Test**
- `node server/server.js` → Should start server and serve `/api/users`
- `npm run dev` in client → Should render something (even a placeholder)
- Test an API call to `/api/users` with `curl` or browser
- Make at least one commit for each of:
  - setting up server
  - adding user model
  - creating API route
  - setting up React app

---

### 4. **Write & Commit the Proposal Report**
Add `docs/proposal-report.md` and commit your proposal (already written).

---

### 5. **Push to GitHub (private repo)**

```bash
git init
git remote add origin https://github.com/your-username/csc-van-app.git
git add .
git commit -m "Initial setup: backend + frontend skeleton"
git push -u origin main
